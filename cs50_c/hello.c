#include <stdio.h> 
/*  #include - serves as a library for standard input and ouput for users (instructs the system to find stdio.h on the local drive)
    stdio.h - A derivative for printf
    .h - Header file(gives you access to libraries)/ Libraries(code someoneelse wrote)
*/

int main(void)
{
    printf("hello, World!\n"); //printf - Function
}

